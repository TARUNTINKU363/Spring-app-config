eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:2525/eureka}
      
      
okta:
  oauth2:
    issuer: https://dev-09697466.okta.com/oauth2/default
    audience: api://default
